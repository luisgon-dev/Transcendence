---
phase: 01-worker-startup-integrity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Transcendence.Service/Workers/ProductionWorker.cs
  - Transcendence.Service/Workers/DevelopmentWorker.cs
  - Transcendence.Service.Core/Services/Jobs/Configuration/WorkerJobScheduleOptions.cs
  - Transcendence.Service.Core/Services/Jobs/Configuration/WorkerSchedulingProfileOptions.cs
  - Transcendence.Service.Core/Services/Jobs/Configuration/WorkerRecurringJobPolicy.cs
  - Transcendence.Service/Program.cs
  - Transcendence.Service/appsettings.json
  - Transcendence.Service/appsettings.Development.json
autonomous: true
requirements:
  - WORK-04
user_setup: []
must_haves:
  truths:
    - Development and production workers derive recurring-job policy from one shared profile-based source.
    - Mandatory baseline jobs are not implicitly removed in development unless an explicit profile override is configured.
  artifacts:
    - Transcendence.Service.Core/Services/Jobs/Configuration/WorkerRecurringJobPolicy.cs
    - Transcendence.Service.Core/Services/Jobs/Configuration/WorkerSchedulingProfileOptions.cs
  key_links:
    - Program.cs binds profile options and injects shared policy consumed by both worker implementations.
    - Both worker classes use the same policy descriptors instead of duplicated per-environment registration branches.
---

<objective>
Establish one shared scheduling policy implementation that both worker environments execute, with profile-driven differences instead of duplicate logic.

Purpose: Eliminate environment drift and make recurring-job behavior deterministic across development and production.
Output: Shared scheduling policy/configuration types, worker refactor to consume them, and profile wiring in host/config.
</objective>

<execution_context>
@/home/luisgon-dev/.codex/get-shit-done/workflows/execute-plan.md
@/home/luisgon-dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-worker-startup-integrity/01-CONTEXT.md
@.planning/phases/01-worker-startup-integrity/01-RESEARCH.md
@Transcendence.Service/Program.cs
@Transcendence.Service/Workers/ProductionWorker.cs
@Transcendence.Service/Workers/DevelopmentWorker.cs
@Transcendence.Service.Core/Services/Jobs/Configuration/WorkerJobScheduleOptions.cs
@Transcendence.Service/appsettings.json
@Transcendence.Service/appsettings.Development.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce shared worker scheduling profile and policy descriptors</name>
  <files>Transcendence.Service.Core/Services/Jobs/Configuration/WorkerSchedulingProfileOptions.cs, Transcendence.Service.Core/Services/Jobs/Configuration/WorkerRecurringJobPolicy.cs, Transcendence.Service.Core/Services/Jobs/Configuration/WorkerJobScheduleOptions.cs</files>
  <action>Create a profile-aware scheduling policy contract that emits recurring-job descriptors (job id, cron source, enabled flag, mandatory baseline membership) and extend schedule options with explicit profile selection/defaults. Keep mandatory baseline explicit and central so environment behavior cannot diverge from hidden worker-specific conditionals.</action>
  <verify><automated>`dotnet build Transcendence.Service.Core/Transcendence.Service.Core.csproj -c Release`</automated></verify>
  <done>Shared descriptor/policy types compile and can represent both production and development schedule outcomes from one code path.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor workers to consume the shared policy instead of duplicated scheduling branches</name>
  <files>Transcendence.Service/Workers/ProductionWorker.cs, Transcendence.Service/Workers/DevelopmentWorker.cs</files>
  <action>Replace duplicated AddOrUpdate/RemoveIfExists scheduling trees with policy-driven iteration that applies the same registration/removal semantics in both workers. Preserve current queue/cancellation expression patterns and cleanup toggles, but remove development-only hard-coded non-analytics removal so profile settings are the sole source of differences.</action>
  <verify><automated>`dotnet build Transcendence.Service/Transcendence.Service.csproj -c Release`</automated></verify>
  <done>Both workers compile against shared policy and differ only by configured profile input, not by hard-coded job lists.</done>
</task>

<task type="auto">
  <name>Task 3: Wire policy configuration in host startup and environment appsettings</name>
  <files>Transcendence.Service/Program.cs, Transcendence.Service/appsettings.json, Transcendence.Service/appsettings.Development.json</files>
  <action>Bind profile options and register policy services in Program startup, then define explicit profile values in appsettings for both environments. Keep development and production aligned by default and express deviations only as named profile overrides to avoid accidental drift.</action>
  <verify><automated>`dotnet build Transcendence.Service/Transcendence.Service.csproj -c Release`</automated></verify>
  <done>Host startup binds and resolves policy services successfully, and both appsettings files carry explicit profile configuration entries.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet build Transcendence.Service.Core/Transcendence.Service.Core.csproj -c Release`
- [ ] `dotnet build Transcendence.Service/Transcendence.Service.csproj -c Release`
- [ ] Development and production worker scheduling paths both execute through the shared policy component.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Recurring-job profile behavior is centralized and deterministic across environments
- Mandatory baseline job policy is represented by shared code, not worker-local branching
  </success_criteria>

<output>
After completion, create `.planning/phases/01-worker-startup-integrity/01-01-SUMMARY.md`
</output>
