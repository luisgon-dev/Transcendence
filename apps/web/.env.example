# Backend base URL for server-side fetching and proxy routes.
TRN_BACKEND_BASE_URL=http://localhost:8080

# API key for AppOnly endpoints (e.g. live game).
# For local dev you can temporarily use Auth:BootstrapApiKey, then replace with a dedicated key.
TRN_BACKEND_API_KEY=trn_bootstrap_dev_key

# Backend timeout for server-side calls and BFF proxy routes (milliseconds).
# TRN_BACKEND_TIMEOUT_MS=10000

# Error verbosity for user-visible messages returned by Next route handlers.
# - safe: generic messages (recommended for production)
# - verbose: include more details (still sanitized, no secrets)
# TRN_ERROR_VERBOSITY=safe

